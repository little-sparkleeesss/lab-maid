---
# VPS 配置示例
# 复制为 vps.yml 并填入真实值

# 如果 VPS 上也需要 SMART 监控
# smart_monitoring_drives:
#   - /dev/sda

# 流量监控 — VPS 上的 vnstat SQLite 路径
traffic_monitor_source_path: /var/lib/vnstat/vnstat.db
traffic_monitor_interface_filter: "ens3"   # 只同步外网接口，避免 Docker/lo 等
